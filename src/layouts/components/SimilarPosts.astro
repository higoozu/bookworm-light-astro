---
import Posts from "@/components/Posts.astro";
import type { CollectionEntry } from "astro:content";

type Props = {
  posts: CollectionEntry<"posts">[];
};

const { posts } = Astro.props as Props;
---

<Posts posts={posts} showSummary={false} showAuthor={false} />
